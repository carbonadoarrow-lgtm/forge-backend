[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    acceptance: acceptance tests for v2 autonomy system
    unit: unit tests
    integration: integration tests
