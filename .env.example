# Backend mode: file | sandbox | prod
FORGE_BACKEND_MODE=file

# Data directory (relative or absolute path)
FORGE_DATA_DIR=data

# CORS allowed origins (JSON array format)
FORGE_CORS_ORIGINS=["http://localhost:3000"]
