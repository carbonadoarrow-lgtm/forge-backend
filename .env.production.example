# Forge Backend - Production Environment Variables
# Copy this to .env for local testing of production config
# For AWS App Runner, set these as Environment Variables in the console

# Environment
FORGE_ENVIRONMENT=prod

# Backend mode
FORGE_BACKEND_MODE=file

# Data directory (will be /app/data in container)
FORGE_DATA_DIR=/app/data

# CORS Origins (JSON array as string)
# Update with your actual Amplify URL after deployment
FORGE_CORS_ORIGINS=["https://main.d1234567890.amplifyapp.com","https://forge-console.yourdomain.com"]

# Port (App Runner uses 8000 by default)
PORT=8000

# API Prefix
FORGE_API_PREFIX=/api

# Log level (optional)
# LOG_LEVEL=info
